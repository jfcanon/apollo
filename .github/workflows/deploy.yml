# Deploys the worker on every push to main, after a fresh test run in this
# job — hermetic on purpose, so the gate holds even if ci.yml changes.
# Doc-only merges skip it. Requires a `production` environment limited to the
# main branch, holding four secrets:
#   CLOUDFLARE_API_TOKEN   Workers deploy rights on the account
#   CLOUDFLARE_ACCOUNT_ID  the Cloudflare account id
#   TIMON_API_KEY          Timon worker API key (Bitwarden item `timonapikey`)
#   TIMON_URL              Timon worker base URL
name: Deploy

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - 'documentation/**'
      - '.claude/**'
      - '.vscode/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy worker
    # workflow_dispatch lets the caller pick any ref, so the job refuses
    # everything but main. The real gate is the `production` environment: its
    # branch rule rejects other refs and the credentials live only there, so a
    # branch that edits this file out of the way still can't reach them.
    if: github.ref == 'refs/heads/main'
    environment: production
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false

      - name: Setup
        uses: ./.github/actions/setup

      - name: Run tests
        run: bun run test

      # Provision the Timon bridge secrets before deploying. Values come from
      # the `production` environment; until the owner sets them this step is
      # skipped and timon_create_task reports "unconfigured" at runtime rather
      # than failing the deploy.
      - name: Configure Timon secrets
        if: ${{ secrets.TIMON_API_KEY != '' && secrets.TIMON_URL != '' }}
        working-directory: apps/agent
        run: |
          echo "$TIMON_API_KEY" | bunx wrangler secret put TIMON_API_KEY
          echo "$TIMON_URL" | bunx wrangler secret put TIMON_URL
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          TIMON_API_KEY: ${{ secrets.TIMON_API_KEY }}
          TIMON_URL: ${{ secrets.TIMON_URL }}

      # wrangler builds the Sandbox container image as part of the deploy,
      # which is why this runs on the runner's Docker daemon.
      - name: Deploy
        run: bun run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
